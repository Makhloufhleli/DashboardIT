{% extends 'base.html.twig' %}

{% block title %}Domains{% endblock %}

{% block body %}
    <h1>Domains</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ domain.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ domain.name }}</td>
            </tr>
            <tr>
                <th>CreationDate</th>
                <td>{{ domain.creationDate ? domain.creationDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>RegistrAr</th>
                <td>{{ domain.registrAr }}</td>
            </tr>
            <tr>
                <th>RegistrAnt</th>
                <td>{{ domain.registrAnt }}</td>
            </tr>
            <tr>
                <th>Ns1</th>
                <td>{{ domain.ns1 }}</td>
            </tr>
            <tr>
                <th>Ns2</th>
                <td>{{ domain.ns2 }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('domains_index') }}">back to list</a>

    <a href="{{ path('domains_edit', {'id': domain.id}) }}">edit</a>

    {{ include('domains/_delete_form.html.twig') }}
{% endblock %}
