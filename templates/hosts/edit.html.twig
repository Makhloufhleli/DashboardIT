{% extends 'base.html.twig' %}

{% block title %}Edit Hosts{% endblock %}

{% block body %}
    <h1>Edit Hosts</h1>

    {{ include('hosts/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('hosts_index') }}">back to list</a>

    {{ include('hosts/_delete_form.html.twig') }}
{% endblock %}
